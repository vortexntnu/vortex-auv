To build the BT *colcon build --packages-up-to orca_simple_bt --symlink-install*

To run the BT *ros2 run orca_simple_bt orca_simple_bt_node*

To build the sim *colcon build --packages-up-to orca_sim_script --symlink-install*

To run the sim *ros2 launch orca_sim_script full_sim.launch.py*
