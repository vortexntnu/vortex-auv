# Message for selecting active cameras in each feed
# Feed specifies which feed one wants to change,
# while camera specifies which camera one wants displayed on this feed
# One message must be sent for each feed/camera change
uint8 feed
uint8 camera
